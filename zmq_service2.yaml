apiVersion: v1
kind: Service
metadata:
  labels:
    app: zmq-service-sub
  name: zmq-service
spec:
  selector:
    app: zmq_sub
  type: NodePort
  externalIPs:
    - 172.21.0.4
  ports:
    - protocol: TCP
      targetPort: 802
      port: 802
